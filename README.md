# Steps to Run a erlang Module

step 1 -> Install erlang in you system.

step 2 -> Open the the folder which contains erlang module (ex -> cd /home/Desktop/Bob).

step 3 -> Open erlang shell (Type erl in your terminal).

step 4 -> Compile the module (c(bob)).

step 5 -> Call the start function (bob:start()).
